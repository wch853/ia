<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.njfu.ia.process.mapper.UploadDataRecordMapper">

    <insert id="batchInsertDataRecords">
        INSERT INTO upload_data_record(end_device_id, data_type, value, receive_time)
        VALUES
        <foreach collection="uploadDataRecords" item="record" separator=",">
            (#{record.endDeviceId}, #{record.dataType}, #{record.value}, #{record.receiveTime})
        </foreach>
    </insert>

</mapper>	